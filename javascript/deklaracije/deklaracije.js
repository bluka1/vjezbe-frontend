/**
// Inicijalizacija varijabli?
*/
var first_name = "Tony";

var first_name = "John's",
lastName = 'Doe',
nadimak = `Johnny ${lastName}`,
price = 0,
discount = 78,
fullPrice = 0,
breakSize = [], // array / niz / lista
$myObject = {}; // object / dictionary / HashMap

console.log(price);
console.log(lastName);
console.log(nadimak);

// Kraj mog programa